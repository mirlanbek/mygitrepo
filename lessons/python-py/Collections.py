# BUL eki stringden 1 dctionery jasait

import collections

val_key = "eki"
vak_val = "2"
disk_sw = collections.OrderedDict()

dicc = {}

disk_sw[val_key] = vak_val

dicc.update(disk_sw)

print(dicc)

