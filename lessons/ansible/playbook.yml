- hosts: vms
  # connection: local

  vars_files:
    - config.yaml

  environment: "{{ proxy_env }}"

  tasks:
    - name: Test
      debug: msg="{{ ansible_system }}"
 
    - debug: msg="{{ ansible_distribution }}"

    # - debug: msg="{{ role_path  }}"


  roles:
    - test_role

